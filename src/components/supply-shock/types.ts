// Placeholder — aguardando dados corretos
export interface BitcoinStats {
  [key: string]: any;
}

export interface TimeRemaining {
  days: number;
  hours: number;
  minutes: number;
  seconds: number;
}
